# v0.2.26 — provisioning reliability and public distribution

- Resumable module and guided setup progress; live rechecks, interrupted states,
  exact per-step retry commands and a completion report.
- Upgrade preview before installer promotion, including saved Decky additions.
- Steam shortcut target/duplicate audit and reviewed, backed-up repairs; Desktop
  icons remain separate from SteamGridDB's Gaming artwork.
- Restore preview, category selection, space/path checks, previous-content backups,
  atomic per-file writes and SHA-256 verification with result journals.
- EmuDeck migration content verification, retained source rollback and checked
  path switching; missing cards cannot silently redirect restored data internally.
- Public GitHub bootstrap, full source workflow and verified release/USB packaging.

The v0.2.25 Decky selections, Android engine, CSS engine/palette and installer
cleanup are preserved. Account/pairing confirmation remains explicit; no credentials
are inspected. Container tests do not replace Steam Deck hardware or live Store
acceptance. Restore is atomic per file; keep apps closed during restore/migration.
