# ADR-002: Module Contract

**Status:** Accepted

Every top-level capability is a module with `module.json`, `README.md`, declared dependencies, supported actions, and tests. `deckctl` orchestrates modules rather than embedding domain-specific logic.
