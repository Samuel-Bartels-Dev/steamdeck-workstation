# ADR-004: EmuDeck Owns Emulator Defaults

**Status:** Accepted

EmuDeck owns general emulator configuration and updates. This project manages installation status, storage selection, BIOS/firmware validation metadata, backup, presentation, and targeted per-game overrides. Do not restore an old global emulator config tree over a fresh EmuDeck installation.
