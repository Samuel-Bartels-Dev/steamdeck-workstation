# Docker readiness and v0.2.28

Deliver the merged storage repair and aliases through a published release, with
guided repair for the observed overlay failure, honest API-vs-launch readiness,
and a short everyday Docker guide.

Also add the requested Zen Browser, Zed, VLC, Plex Desktop and Spotify Flatpaks
to Utilities/Development/Media, with idempotent installs and presence verification.
The baseline's cumulative reviewed-change list adds exactly those nine module
install/verify/README paths; historical hashes and permissions remain intact.

Keep status read-only. Bind timestamped test results to engine/config/fixture and
invalidate prior success on interrupted/failed tests. Offer storage repair only
with explicit confirmation, a host helper and no existing managed containers;
never switch remote engines or stop user workloads automatically.

Validate contracts, static checks, real Deck containers/builds, release archives,
GitHub CI and the published bootstrap download before completing the release.
