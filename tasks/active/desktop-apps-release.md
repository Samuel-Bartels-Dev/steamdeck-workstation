# Desktop apps release

Add Zen to Utilities, Zed to Development, and VLC, Plex Desktop and Spotify to Media through user Flathub installs. Preserve existing apps and web shortcuts. Verify presence without mutation; propagate failures and retry safely.

Release v0.2.29 follows the already published Docker release. Nine reviewed module paths (README.md, install.sh and verify.sh in each affected module) join the existing cumulative baseline exceptions; historical hashes and modes remain intact.

Validate contract tests, repository checks, static checks, release packaging and live package presence. Login, playback and editor integrations are outside installation verification.
