# Familiar Docker commands

Add Docker and Compose aliases with short forms for interactive Bash/Zsh users:
docker/d and docker-compose/compose/dc. Own the mapping and provisioning in the
container integration. Existing executables, functions and aliases take priority.
Keep arguments, working directory, exit status and selected-engine routing intact.
Do not start services merely to install aliases or modify the SteamOS image.

Cover shell sourcing/repeated installation, native command conflicts and actual
argument forwarding. Validate the
repo, run static checks and check the aliases against Docker on the Steam Deck.
