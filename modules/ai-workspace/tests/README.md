Run `python3 tests/contract/test_ai_workspace.py`. Tests need loopback sockets and
use isolated fake engines, process groups, temporary homes and configs. No vendor
installation or paid model API is called.
